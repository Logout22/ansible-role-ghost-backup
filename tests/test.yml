---
- hosts: localhost
  remote_user: root
  roles:
  - role: geerlingguy.nodejs
    nodejs_version: "10.x"
    nodejs_npm_global_packages:
      - name: ghost-cli
  - role: ansible-role-ghost-backup
